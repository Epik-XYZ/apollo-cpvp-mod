plugins {\n    id 'fabric-loom'\n}\n\nrepositories {\n    mavenCentral()\n    maven { url 'https://maven.fabricmc.net/' }\n}\n\ndependencies {\n    // Add Meteor Client dependency here\n    modCompile 'xyz.meteorclient:meteor-client:<version>'\n    modCompile 'org.spongepowered:mixin:0.8.5'\n}\n\ntask remapJar(type: com.github.johnrengelman.shadow.tasks.ShadowJar) {\n    archiveBaseName.set('apollo-cpvp-mod')\n    archiveVersion.set('1.0.0')\n    manifest {\n        attributes 'ModVersion': '1.0.0', 'MixinConfigs': 'mixins.apollocpvp.json'\n    }\n}